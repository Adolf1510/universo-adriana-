<Window x:Class="BoticaPOS.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BoticaPOS Login" Height="220" Width="380">
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
        <TextBlock Text="Usuario"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="2" Content="Ingresar" Margin="0,12,0,0" Command="{Binding LoginCommand}"/>
        <TextBlock Grid.Row="3" Text="{Binding Status}" Foreground="DarkGreen"/>
    </Grid>
</Window>
